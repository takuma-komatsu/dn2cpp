# STATUS — open backlog

Open tickets only. Closed work is not tracked here — delete the row when it is
done.

## Conventions

**A ticket id is a temporary handle.** It lives in this file and nowhere else:
not in source, not in a comment, not in a gate script, not in another document,
not in a commit message. A ticket that lands has its row deleted and its id goes
with it, so nothing outside this file can be left pointing at a number that
stopped meaning anything. A constraint worth keeping past the ticket is written
out as an invariant at the site it constrains, or as a boundary in `README.md` —
never as a reference back to a row.

- **Ids are ascending integers and are never reused.** A deleted row's number
  stays spent; the next ticket takes the next integer above every number this
  file has ever issued.
- **A permanent boundary is not a ticket.** The things dn2cpp will not do — the
  AOT and platform limits it shares with the IL2CPP family — are the *Permanent
  non-goals* section of `README.md`. Read it before filing, so a settled boundary
  does not come back as a row.
- **Write the row so somebody else can act on it**: what to build, where the
  design lives, and which of its numbers must be re-measured rather than trusted.

## Open

| # | area | ticket |
|---|------|--------|
| 1 | godot-editor | **Notarize the macOS editor.** The `.app` is packaged and ad-hoc signed (`dist/package-editor-macos.sh`), so what remains is the notarization story: a Developer ID identity, `--options runtime`, and a re-audit of the fork's `misc/dist/macos/editor.entitlements` against what the export backend actually does at export time — spawn the host's `clang++` (external) and the bundle's own `cmake`, `ninja`, `node` and Emscripten `clang` (internal, hence part of the notarized payload). Audit by running a hardened build and measuring which entitlement each spawn needs, not by reading the existing list. The signing set needs no code change: the packager enumerates every Mach-O under the `.app` via `file -h -F '\|'` and signs each before sealing, so the bundled tools are already covered and only the printed count moves. One ordering trap — `lipo -thin` invalidates any signature it rewrites, which is harmless only because staging runs before signing. |
| 7 | godot-editor | **Sign the Windows editor.** `dist/package-editor-windows.sh` ships it unsigned and says so in its metadata, so SmartScreen reports an unknown publisher and the download's whole identity is `SHA256SUMS.txt`. What is needed: a code-signing certificate, `signtool` on the packaging host, and a decision about the signing set — the two editor binaries alone (`Godot-dn2cpp.exe` and its `.console.exe`), or every PE the bundle carries with them: `Dn2Cpp/bin/dn2cpp.exe`, `buildtools/` cmake, cmcldeps and ninja, and the Emscripten SDK's own executables. The decision is the same for all of them, so make it once. The macOS notarization row above is this one's counterpart, and neither answers for the other's platform. |
| 18 | godot-editor | **The Windows preflight still passes a bare `cl.exe` once the MSVC import has failed.** The fork's export backend now locates an installation through vswhere, runs `vcvarsall` itself and hands the resulting `INCLUDE`/`LIB` and a pinned `CMAKE_CXX_COMPILER` down to the build, so the ordinary machine with Visual Studio installed and no Developer Command Prompt exports fine and is no longer the case to fix. What is left is the fallback arm: `HostCxxCompiler()` answers `msvc?.ClExe ?? OS.PathWhich("cl") ?? OS.PathWhich("clang++")`, and the middle term is reached exactly when the import found nothing — a hand-assembled PATH carrying `cl.exe` with no vswhere or `vcvarsall.bat` in reach. There the preflight clears and cmake's compiler check dies instead, which is the failure the preflight exists to turn into an actionable refusal. `gates/_common.sh` spells the condition and the reason with it — *"cl.exe on PATH does not imply vcvars ran; without LIB link.exe is lost"*, testing `cl.exe` **and** `${LIB}` **and** `${INCLUDE}` — and it belongs on that arm alone, so an import that succeeded is not re-litigated. **Verifiable on a Windows host only.** |
| 50 | intrinsics | **Platform ISA contract: the host-bound and compiler-surface follow-ups.** Every family with feature bits is Lowered (the whole `X86.*`, `Arm.*` and `Wasm.*` surfaces; `Sve` and `Sve2` have no feature bits and answer false by design), so what remains is verification. Host-bound: a manual run of `./gates/build-and-run-platform-isa-native.sh` on a provisioned windows-x64 host with `DN2CPP_SKIP_PLATFORM_ISA_NATIVE` unset must validate S across the full supported surface, including zero-divisor versus quotient-overflow `DivRem` exceptions, full-range SSE lane-index wrapping, and opaque VCMPSS/VCMPSD upper-lane preservation, then complete O (`DN2CPP_ENABLE_AVX10V2=1` vs `DOTNET_EnableAVX10v2=1` when the compiler can build it) and P (`DN2CPP_CPU_FEATURES=-Avx` vs `DOTNET_EnableAVX=0`, with X86Base kept and Bmi1 removed). Hosted Windows smoke excludes this gate. Linux-x64 S/O/P remain unverified. No AVX-512 or AVX10 instruction body has executed on supporting hardware: `tools/platform-isa-rosetta.sh` runs the probe under Rosetta 2, which has no AVX, and is a developer procedure without the .NET oracle, not a gate. Compiler-surface: the AVX10.2 rows use current Clang intrinsic spellings; their compiler-capability guard makes the affected tokens false under GCC, MSVC and older Clang, so adding and verifying each compiler's spellings remains open. |
| 51 | runtime | **Reflection invoke does not validate its receiver and arguments the way .NET does.** `dn2cpp_invoke_row` (`runtime/core/intrinsics/dn2cpp_system_reflection.cpp`) raises `NullReferenceException` for an instance method invoked with a null receiver, and never tests a non-null receiver against the declaring type; .NET throws `TargetException` for both. The boxed invoker thunks that `EmitInvokerThunk` (`src/Dn2Cpp.Transpiler/CppEmitter.cs`) emits read each argument as the parameter's representation with no type test, so a mismatched box or a reference of the wrong type is undefined behaviour where .NET throws `ArgumentException`, and a null argument for a value-type parameter is dereferenced where .NET passes the zero-initialized value. All of these are raised before the target runs, so none is wrapped in `TargetInvocationException`. The argument-count `ArgumentException` (.NET: `TargetParameterCountException`) is the same family and is frozen by `ReflectIndexerEdgeSubset` in `gates/build-and-run-reflect-types.sh`; a fix that models `TargetParameterCountException` moves those lines to the live diff. |
| 52 | runtime | **`Delegate.Method` misses .NET's answer where the delegate's static identity cannot reach the bound method's row.** `dn2cpp_delegate_get_method` (`runtime/core/intrinsics/dn2cpp_system_reflection.cpp`) resolves the declaring row, then answers an interface or generic-virtual binding from the receiver case the emitter recorded; an interface identity records every emitted allocated receiver, the declaration's own default body included. A `MakeGenericType` instantiation answers a generic-virtual binding from its template level's case, as its own level of that case's declaring definition declares it; its method rows name the instantiation. A receiver without a case — such an instantiation under an interface binding, or a type the image never allocates — is resolved at run time by slot address and name: its class levels, then the derived-interface overrides its interface table lists, then the declaration's default. The remaining gaps are: (a) a runtime-owned or opaque declaring type with no rows returns null — `Object` virtuals such as `obj.ToString`, `String` and `Math` statics, intrinsic-mapped owners; (b) an interface binding on a runtime-owned receiver (a boxed primitive, string, or array) can report the interface declaration instead of the implementation; (c) the run-time resolution reports the declaration when it cannot distinguish explicit implementations of two instantiations of one generic interface, and when an unrecorded value-type receiver's body is a derived-interface override, whose address the unboxing thunk hides; (d) under `--trim-reflection` an answer needs the selected method's declaring type, so `PlatformNotSupportedException` comes from a stripped class level that overrides a virtual slot, a recorded interface or generic-virtual target on a stripped type (a class implementation or a derived-interface default), and, for a receiver without a case, a stripped class level, a stripped implemented interface deriving from the declaring interface, or a stripped level an unrecorded class generic-virtual binding passes. `ReflectDelegateIdentitySubset` in `gates/build-and-run-reflect-invoke.sh` accepts null for (a) and is where exact answers get asserted. The Delegate.Method section of `gates/build-and-run-trim-reflection.sh` asserts the (d) throws for recorded targets and that a declaration default still answers beside a stripped unrelated derived interface; the receiver-without-a-case throws have no witness there. |
| 55 | runtime | **Delegate equality can merge distinct virtual methods whose code addresses fold.** `dn2cpp_delegate_identity_equal` treats two virtual bindings with equal target, function address and method arguments as equal even when they bind different methods; `Delegate.Remove` then removes the wrong entry. Comparing declaration owners or vtable slots alone is also wrong: .NET equates base/override and class/interface declarations bound to one implementation, and distinguishes separate new-slot or explicit implementations. Emitted receiver cases identify the selected interface and generic-virtual methods for `Delegate.Method`; equality does not compare them, and class virtual bindings have no equivalent selected-method key. Supply a key for every binding, including when receiver reflection metadata is trimmed, then diff equality and removal against .NET under code-address folding. |
| 61 | ildiet | **ILDiet removes the instance constructors of an application type named only by a custom-attribute `Type` argument.** Once a retained body calls non-generic `Activator.CreateInstance` or `ConstructorInfo.Invoke`, ILDiet keeps the instance constructors of every application type a retained body names with `ldtoken` (`NoteTypeToken` in `src/ILDiet/AssemblyDiet.cs`), but `MarkArgument` marks an attribute's `Type` values without recording them. `[Make(typeof(Target))] class Holder` followed by `Activator.CreateInstance(attribute.T)` therefore keeps `Target` and strips `Target::.ctor`, where .NET and `--no-ildiet` construct it. Record attribute `Type` arguments (constructor arguments, fields and properties, nested arrays included) in the same keep set, and assert an attribute-selected type in `ILDietControl`, which `gates/build-and-run-preserve-control.sh` diffs with ILDiet on. |
| 62 | runtime | **`Type.Name` of a nested generic instantiation reports its C++ readable name.** `typeof(Outer.Box<string>).Name`, `GetType().Name` on such an instance and `Delegate.Method.DeclaringType.Name` for its methods answer `Outer_Box_String` where .NET answers ``Box`1``; a top-level generic instantiation answers correctly. Diff `Name`, `FullName` and `ToString()` of nested generic instantiations (class and struct, one and two enclosing levels, a generic enclosing type) against .NET in the reflect-types bucket. |
| 63 | runtime | **A delegate over an ambiguous default interface slot throws when invoked instead of when created.** .NET's `ldvirtftn` resolves the slot and throws `AmbiguousImplementationException` while the delegate is created, for a plain and a generic interface method alike. dn2cpp's `ldvirtftn` loads the slot's throwing stub or the generic-virtual dispatcher, so the exception comes from the first `Invoke`, and a delegate that is never invoked never throws. Detect the ambiguous slot at the `ldvirtftn` site and throw there, then extend `gates/build-and-run-ambiguous-default-interface.sh` with delegate creation for both shapes. |
| 64 | runtime | **The ambiguity message spells a generic interface method's declaring type differently from .NET.** For a generic method on a generic interface, .NET's `AmbiguousImplementationException` message names the interface with the method's own type parameter (``IBase`1[U]``), where `AmbiguousImplementationMessage` (`src/Dn2Cpp.Transpiler/CppEmitter.AmbiguousImplementation.cs`) writes ``IBase`1[System.__Canon]``. Measure .NET's rule, including when the interface and method generic arities differ, before reproducing it, then add the shape to `gates/build-and-run-ambiguous-default-interface.sh`. |
| 65 | runtime | **A `MakeGenericType` instantiation shares its template's field and property rows.** Its method and constructor rows name the instantiation, but `FieldInfo.DeclaringType` reports the runtime template, and `typeof(Box<>).MakeGenericType(typeof(int)).GetProperty("Name")` returns null where .NET returns the property. Give the instantiation its own field and property rows, with accessors pointing at its method rows, and diff against .NET in `ReflectRuntimeInstantiationSubset` (`gates/build-and-run-reflect-types.sh`). |
| 66 | transpiler | **A constrained call to a static virtual member with sibling derived-interface overrides runs the declaration's default.** When two unrelated interfaces deriving from the declaring interface each explicitly implement the member and the constrained type has no body of its own, `ResolveStaticVirtualImpl` (`src/Dn2Cpp.Transpiler/Compilation.Resolve.cs`) finds no most specific override and returns null. The call then binds the declaration's default body, or transpilation stops with the no-IL-body diagnostic when the member has none. .NET throws when the call runs: `AmbiguousImplementationException` for a generic member and `InvalidProgramException` for a non-generic one. C# rejects the shape (CS8705), so compile the application against a library version whose second interface overrides nothing and run it against the next version, then match .NET's exception for a generic and a non-generic member, with and without a default body. |


## Regression gate

The full gate suite (`gates/build-and-run-*.sh`, each a themed multi-section
program) is the regression gate — run it through the parallel runner before
committing:

```bash
./gates/run-all-gates.sh               # all gates (pre-build once → parallel; Godot in chains)
SKIP_GODOT=1 ./gates/run-all-gates.sh  # skip Godot for a faster smoke check
DN2CPP_REQUIRE_ALL=1 ./gates/run-all-gates.sh   # every gate must RUN
```

**A skip is not a pass.** A gate whose optional prerequisite is absent (an
Android NDK, an Xcode simulator, Emscripten, a scons-built Godot editor) opts out
via `gate_skip` and is counted and reported **separately** — the summary says how
many gates skipped and why, and never claims "all N passed" when some of the N
never ran. `DN2CPP_REQUIRE_ALL=1` turns any skip into a failure and refuses
cached partials; that is the mode a merge runs. This is not hypothetical: a whole
lane once shipped red behind a gate that skipped on every machine lacking its
artifacts — the default — while the suite reported all-green.

A failing gate is listed in `$LOGDIR/_failures.txt` with its log, a skipped one
in `$LOGDIR/_skips.txt` with its reason. For a quick manual smoke check of the
three lanes, run the representative trio directly:

```bash
./gates/build-and-run-sample.sh          # console: C# → IL → C++ → native
./gates/build-and-run-multiassembly.sh   # multi-assembly (-r)
./gates/build-and-run-godot-sample.sh    # Godot GDExtension, real engine
```

The runner discovers gates by glob, so a new `build-and-run-*.sh` is picked up
automatically — there is no list to maintain here. `AGENTS.md` is the
authoritative contract for the suite, the skip protocol, the result caches and
the merge gate.
