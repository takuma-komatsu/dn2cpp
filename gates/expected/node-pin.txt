# The Node.js the Emscripten SDK carries, so a user's machine needs none.
# gates/setup-emsdk.sh reads this file and refuses an archive whose sha256 does
# not match. Rows: `archive <host> <url> <sha256> <strip>`.
#
# emcc is not merely a python launcher: every link runs node (compiler.mjs
# --symbols-only, `node --check`, preprocessor.mjs). An SDK carrying none takes
# the host's, whose version this repository cannot promise anyone.
#
# A pin of its own rather than a row in emsdk-pin.txt: nodejs.org is a third
# upstream, and that file's base_url + path would become a fiction.
#
# `.tar.gz` on POSIX, though nodejs.org publishes `.tar.xz` beside it: unpacking
# then needs no xz arm. `strip` is 1 for every row — each archive roots in one
# `node-v<version>-<host>/`.
#
# An LTS line, never current: this binary ships to users, and gates/_run_in_chrome.js
# assumes global fetch and WebSocket (Node 22 and later).
version  24.19.0

archive macos-arm64 https://nodejs.org/dist/v24.19.0/node-v24.19.0-darwin-arm64.tar.gz 8294b7aa9b03997481c06babf1e8b270c859358f27da57a11509afe537ac381d 1
archive linux-x64   https://nodejs.org/dist/v24.19.0/node-v24.19.0-linux-x64.tar.gz     f625d97cd707df4ff96254916fbc5ff014f09c09effe5a1e0ca8f6d41a8789d4 1
archive windows-x64 https://nodejs.org/dist/v24.19.0/node-v24.19.0-win-x64.zip          57f71ab3652e797d84acddc79c81cc9ff1c6ddb2a1974cdb83f00fee9bff4c73 1
