# CLAUDE.md — dn2cpp

Working instructions for Claude Code in the **dn2cpp** repository. The full
contract — build, gates, module boundaries, code style — lives in `AGENTS.md`
and is imported below; `README.md` has the architecture overview, `docs/` the
design docs and the open backlog (`docs/STATUS.md`).

@AGENTS.md
